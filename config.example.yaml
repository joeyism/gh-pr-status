orgs:
  - mycompany
  - other-org
poll_interval: "30s"
sort_by: "updated"
sort_dir: "desc"
# Optional interactive command run by Enter on a PR.
# ${PR_URL} is replaced with the parent PR URL (shell-quoted).
on_enter: 'gh code-review ${PR_URL}'
